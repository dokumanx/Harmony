class Location {
  // TODO: According to type of LocationService.location() change [_currentLocation] and [_initialLocation] type
  final double currentLocation;
  final double initialLocation;
  final double allowedDistance;

  Location({this.currentLocation, this.initialLocation, this.allowedDistance});
}
